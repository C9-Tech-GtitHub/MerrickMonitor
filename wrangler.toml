name = "merrick-monitor"
main = "worker-auth.js"
compatibility_date = "2024-11-27"

# Routes for the worker
routes = [
  { pattern = "merrick-monitor.c9-dev.com/*", zone_id = "a741eecfab81e43bf0dc0a44545202f5" }
]

# Environment variables for credentials
[vars]
# These will be overridden by secrets set via wrangler secret put
# AUTH_USER = "admin"
# AUTH_PASS = "your-secure-password"
