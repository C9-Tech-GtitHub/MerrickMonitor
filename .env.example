# GitHub Personal Access Token (optional but recommended)
# Without this, you'll hit GitHub API rate limits (60 requests/hour)
# With a token, you get 5,000 requests/hour
#
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token" -> "Generate new token (classic)"
# 3. Give it a name like "Merrick Monitor"
# 4. Select scopes: "repo" (for private repos) or "public_repo" (for public only)
# 5. Click "Generate token"
# 6. Copy the token and paste it below

VITE_GITHUB_TOKEN=your_github_token_here

# Google OAuth Client ID (required for Google Sign-In)
# To create credentials:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create a new project or select an existing one
# 3. Click "Create Credentials" -> "OAuth client ID"
# 4. Configure OAuth consent screen if prompted
# 5. Application type: Web application
# 6. Add authorized JavaScript origins:
#    - http://localhost:5173 (development)
#    - https://your-domain.com (production)
# 7. Copy the Client ID and paste it below

VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# ============================================
# Grafana Cloud Telemetry (Cloudflare Environment Variables)
# ============================================
# These are set in Cloudflare Pages Dashboard, NOT in this file.
# Go to: Cloudflare Dashboard -> Workers & Pages -> merrick-monitor -> Settings -> Environment Variables
#
# GRAFANA_PROMETHEUS_URL=https://prometheus-prod-<region>.grafana.net/api/prom
# GRAFANA_INSTANCE_ID=your_instance_id
# GRAFANA_API_TOKEN=your_api_token
#
# To get these values:
# 1. Sign up at https://grafana.com/auth/sign-up/create-user (free tier)
# 2. Create a stack -> Go to Prometheus details
# 3. Note the remote read URL and Instance ID
# 4. Generate an API token with MetricsPublisher scope
